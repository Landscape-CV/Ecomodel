# Ignore all generated assets, pointclouds, and scenes
# Ignore all generated assets, pointclouds, and scenes
output/
testdataset/

# Point-SAM fine-tune artifacts (large binaries; keep training code)
pointsam_voxelized/
pointsam_checkpoints/

# TreeLearn fine-tune artifacts (large binaries; keep training code)
treelearn_prepared/
treelearn_checkpoints/

# Ignore Python cache files
__pycache__/
*.pyc

# Ignore machine-specific configurations
configs/pipeline_config.json
configs/mangrove_config.json
configs/arbaro.xml

# Ignore downloaded binaries/libraries
lib/
